{
    "name": "Lore-viz",
    "version": "1.0.0",
    "description": "Webapp for showing LORE_sa output in an interactive way",
    "main": "script.js",
    "scripts": {
        "start": "serve -s . -p 8080 --no-request-logging && cmd /c start http://localhost:8080",
        "serve-only": "serve -s . -p 8080 --no-request-logging"
    },
    "dependencies": {
        "d3": "^7.8.5"
    },
    "devDependencies": {
        "serve": "^14.2.0",
        "dependency-cruiser": "^16.10.4"
    }
}
