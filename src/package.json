{
    "name": "Master Thesis",
    "version": "1.0.0",
    "description": "Webapp for showing LORE_sa output in an interactive way",
    "main": "script.js",
    "scripts": {
        "start": "python main.py",
        "start-client": "serve -s . -p 8080 --no-request-logging && cmd /c start http://localhost:8080",
        "serve-only": "serve -s . -p 8080 --no-request-logging",
        "postinstall": "pip install -r requirements.txt"
    },
    "dependencies": {
        "d3": "^7.8.5"
    },
    "devDependencies": {
        "serve": "^14.2.0",
        "dependency-cruiser": "^16.10.4"
    }
}
