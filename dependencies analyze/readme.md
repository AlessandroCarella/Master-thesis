enter the folder ```Master-thesis``` in a ```cmd```

run the following commands:

```npx dependency-cruiser --init```
(1. ```src```, 2.```n```)

```npx depcruise src --output-type json > deps.json```

Replace the generated files in this folder

Open the ```depencency.html``` file with live server

